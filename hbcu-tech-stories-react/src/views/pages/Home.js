
import React, { useState } from "react";
import StoryGrid from "../components/story-grid";

const Home = (props) => {
    return (
        <>
            <StoryGrid />
        </>);
};

export default Home;